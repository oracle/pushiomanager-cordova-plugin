repositories{
    mavenCentral()
}

dependencies {
    compileOnly fileTree(dir: '../PushIOManager', include: ['oracle-cx-mobile-base-7.0.1.aar'])
    compileOnly 'com.google.firebase:firebase-messaging:18.0.0'
}

android {
    packagingOptions {
        exclude 'META-INF/NOTICE'
        exclude 'META-INF/LICENSE'
    }
}
